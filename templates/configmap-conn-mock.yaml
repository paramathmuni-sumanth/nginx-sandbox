{{- if .Values.connectionMock.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nginx-sandbox.fullname" . }}-conn-mock
  labels:
    {{- include "nginx-sandbox.labels" . | nindent 4 }}
data:
  server.py: |
{{ .Files.Get "poc/mock-open-connections.py" | indent 4 }}
{{- end }}
